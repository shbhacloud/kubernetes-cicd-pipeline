apiVersion: v1
kind: Service
metadata:
  name: kubernetes-cicd-pipeline
  labels:
    app: kubernetes-cicd-pipeline
spec:
  type: NodePort
  selector:
    app: kubernetes-cicd-pipeline
  ports:
    - name: http
      port: 80
      targetPort: 5000
      nodePort: 30080

